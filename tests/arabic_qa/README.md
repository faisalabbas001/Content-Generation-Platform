# Arabic QA eval harness

- `calibration_20.json` — small curated set used during CCO bring-up.
- `eval_50.json` — formal M2 gate; must reach ≥80% agreement with ground truth.

Run: `pnpm test:arabic-qa`.

