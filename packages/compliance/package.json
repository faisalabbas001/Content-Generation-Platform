{
  "name": "@repo/compliance",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic pre-generation Compliance Gate (Spine) — LLM-free hard-block / negpat / cultural-gesture enforcement",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo no-lint"
  },
  "dependencies": {
    "@repo/db": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
