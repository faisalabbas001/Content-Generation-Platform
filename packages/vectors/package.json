{
  "name": "@repo/vectors",
  "version": "0.0.0",
  "private": true,
  "description": "Qdrant wrapper — per-brand namespaces for CaptionContext caching (Doc §3.2, §5.3)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "echo no-lint"
  },
  "dependencies": {},
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5.5.0"
  }
}
