{
  "name": "@repo/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Arabic unicode guard, Hijri dates, Arabic slug transliteration",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5.5.0"
  }
}
