/**
 * DEPRECATED — do not use.
 *
 * This was the old v6 single-page onboarding action (submitOnboarding).
 * The page that used it (/[slug]/onboarding) now redirects to /onboarding-start.
 * The active actions are in onboarding-v2.ts (submitSeed + submitFinal).
 *
 * Nothing should import from this file. It is kept empty to prevent accidental
 * recreation of the old flow.
 */
export {}
